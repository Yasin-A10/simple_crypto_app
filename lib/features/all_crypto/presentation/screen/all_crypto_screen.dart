import 'package:flutter/material.dart';

class AllCryptoScreen extends StatelessWidget {
  const AllCryptoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('All Crypto Screen'),
    );
  }
}